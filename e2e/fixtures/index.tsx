import { registerRootComponent } from 'expo';
import { SceneFixtures } from './SceneFixtures';

// Deliberately separate from ../../index.ts and the production App.
registerRootComponent(SceneFixtures);

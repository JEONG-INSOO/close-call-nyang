# Task: T03 실제 랭킹 서버 연결과 운영 검증

## Status: done

## Goal
사용자의 Supabase 프로젝트에 준비한 API/스키마를 적용하고 실제 두 익명 플레이어로 공유 순위·권한·삭제·실패 복구가 동작하는 것을 확인한다. 운영과 개인정보 변경을 다음 출시 단계에 전달한다.

## Decision Summary
- 온라인 기능은 mock 테스트만으로 완료하지 않는다. 무료 범위로 시작하되 실제 계정 연결은 실행 시 사용자와 진행하며 요금제 결제/상향은 별도 선택이다.
- 현재 계획 단계에서는 외부 프로젝트나 자격증명을 만들지 않는다. 실제 랭킹 QA 완료 후 P03 출시로 이동한다.

## Implementation

### I01. 프로젝트·인증·서버 설정
- Related Files:
  - `supabase/config.toml`, `supabase/migrations/`, `supabase/functions/` :: T01 server — modify only observed issues
  - `.env.example`, `.gitignore` :: public/server env boundary — modify
  - `scripts/verify-leaderboard.mjs` :: hosted smoke runner — new
  - `scripts/inspect-public-env.mjs` :: forbidden secret/config validation — new
  - `package.json` :: ranking:verify, ranking:env-check — modify
  - `docs/leaderboard-operations.md` :: project/config/retention/moderation/recovery — new
  - `docs/qa-report.md`, `docs/learning-notes.md` :: actual evidence — modify
#### Details
- **Signatures & Types**:
  ```typescript
  export interface HostedRankingCheck {
    id: string; status: 'passed' | 'failed' | 'not_run'; checkedAt: string;
    projectRef: string; environment: 'staging' | 'production'; evidence: string;
  }
  // node scripts/verify-leaderboard.mjs --environment staging|production
  // node scripts/inspect-public-env.mjs
  ```
- **Data & Schema Fields**:
  - Project ref/region/public URL/public key are nonsecret operational data. Native/web build variables: EXPO_PUBLIC_SUPABASE_URL,EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY. DB password, Supabase access token, server secret/service role key never use EXPO_PUBLIC prefix; never echo/commit. Use actual existing project if appropriately dedicated, no guessing from GitHub username.
  - Default production `JEONG-INSOO/close-call-nyang` web origin is `https://jeong-insoo.github.io`, not including path. Staging additionally allows local origins. Native requests may have no Origin: public GET leaderboard is available without identity under anonymous read limits, protected routes still require a verified JWT. Separate staging/production refs and auth users; tools must show which before any schema write.
  - Supabase Auth anonymous sign-in enabled, normal email/social UI not integrated. Confirm built-in signup request limits; configure supported bot protection if required/available for Expo/web without breaking signup. If CAPTCHA is enabled, implement its actual verified token flow in nickname participation, not a fake token or disabling auth checks. Record service limits and unresolved abuse defenses honestly.
  - Cleanup scheduled SQL daily: active/terminal runs idle>24h, finalized receipts>7days, reports>90days, rate buckets>24h, completed deletion tombstones7days after completed_at and only after the configured JWT maximum lifetime has elapsed. Never clean pending_auth_delete tombstones before completing Auth removal. Keep best scores/profile until user deletion/moderation; don't blanket-delete anonymous users because they are older than a few days. Logs follow actual provider retention, not fictitious zero logging.
- **Execution Flow / Logic**:
  1. Read-only check actual CLI availability/current Supabase deployment docs and authenticated account. Obtain project choice/login at execution. Prepare local code and review schema before requesting missing access. Use Free only if available; stop before any billing action requiring new spending choice.
  2. Validate canonical sync/tests, then link correct staging ref and apply migrations using Supabase CLI. Hosted deploy can use server-side bundling when no Docker; confirm supported `functions deploy --help` flags rather than assume installed option. Apply only intended migrations, not arbitrary database reset. Existing nonempty project requires read-only schema inspection and conflict-safe migration.
  3. Deploy leaderboard-api with server env and app-specific CORS/rate-limit/limits. Gateway optional JWT behavior must allow public GET while handler explicitly verifies all writes with auth.getUser. If gateway config disables built-in verify_jwt for public access, it does **not** disable handler validation; test every write route without/with wrong JWT.
  4. Run staging tests including generated legal replay fixtures, negative ownership attempts, simultaneous calls and delete cleanup. The runner must pace legal chunks against the actual challenge issued_at and elapsed wall-clock budget, including the3sec countdown; immediately uploading precomputed future ticks would correctly fail the anti-speed check. Use short interruptible waits and report progress for long fixtures. Tests use dedicated temporary guest users created by this test and delete only their own profile via the normal API after success/failure. Never enumerate/delete other users or change real records to manufacture evidence.
  5. Apply proven migration/server to actual production project, set actual public build vars in ignored local env and record nonsecret operational values. CI workflow/eas.json do not exist yet: hand off their actual variable injection to P03-T01/T02 rather than create those files here. Production smoke creates disposable guest profiles with known names, posts a valid time-paced legal input proof to the real verifier (no bypass flag/test-score endpoint), reads rankings and deletes those exact profiles. Record sanitized request IDs/outcomes without tokens. Require explicit --environment target to prevent confusion.
  6. `ranking:env-check` checks built public config has HTTPS URL and publishable key type, no known server env names/secret assignments in public config/dist, production mockAds disabled. Never print secret match values. Text scan is supplemental to architectural key separation, not proof that arbitrary secrets are impossible.
  7. Free service currently has quotas/inactivity pausing; document monitoring/restoration/export backup procedure, not paid SLA or infinite availability. Local gameplay and error UI must stay usable if API paused or quota exceeded. No paid upgrade/keepalive abuse to evade service limits.

### I02. Real permission/replay/ranking verification and handoff
- Related Files:
  - `scripts/verify-leaderboard.mjs` :: actual REST/anonymous Auth tests — new
  - `docs/leaderboard-operations.md` :: admin procedures and privacy inventory — new
  - `docs/qa-report.md` :: cloud cases/cross-runtime hashes — modify
  - `src/online/`, `supabase/` :: fix observed integration issues — targeted modify
#### Details
- **Execution Flow / Logic**:
  1. Two fresh anonymous sessions A/B may choose same nickname. A cannot change B's name/run or best via direct REST/RPC; unauth and public key alone cannot write. Wrong JWT/signature, tampered userId, guessed UUID and CORS bypass fail appropriately. Check service-only RPC grants against actual deployed DB, not just mocked repository.
  2. Submit legal replay with server seed, verify accepted integer score and best. Tampered score field rejected/ignored by strict schema; same last chunk retry no duplicate, same seq changed input fails, wrong owner/expired/version/after-fall data rejected. Duplicate finalize and concurrently finishing valid runs preserve max and earliest equal-score time.
  3. Check ties rank(1,1,3) and top30/me (user-approved cap correction) using staging fixtures seeded via controlled migration/test role if generating31 completed runs is impractical; label fixture-based rank test separately from full end-to-end real playback. Production test never inserts synthetic points via admin role.
  4. Verify browser interaction on real staging API and a separate native/Expo Go session if available; full production iPhone check remains P03-T03. In this Task backend semantics must pass independent of native availability; record Hermes fixture result pending if phone unavailable and carry explicit P03 requirement. Compare Node/Deno/browser numeric golden hashes now.
  5. Disconnect during playing: same local outcome, bounded pending badge; reconnect flush and finalize; offline-start never backfills; corrupted queue/stale challenge/user delete does not attach to new run. Simulated network tests supplement actual network call evidence.
  6. Rename preserves score, report stored with rate limit, hide removes row only for blocker, banned row omitted consistently from ranks. Delete removes public record and Auth guest user, retry after lost response yields safe settled state. Session reset doesn't auto-recreate profile. Manual admin moderation steps identify exact target publicID and audit action/time, never embed admin credentials in app.
  7. Privacy inventory to P03: public nickname/score, private guest ID/session, server-issued run state/digest/receipt, short-lived proof handling, reports, provider request logs. Separate purposes/retention/deletion/support. Supabase operator identity/region and service disclosure use actual values. App Store “no data collected” must be reevaluated, not copied from old offline plan.
  8. If account/project/permission missing, keep task pending with exact missing prerequisite and prepared files. Hosted failure cannot be replaced by mocked passing report. Once actual checks pass, use phase/current protocol to advance P03.

## Acceptance Criteria
- [x] Actual target Supabase environment deployed with verified policies, anonymous identity and strict ownership.
- [x] Valid replay produces public ranking, malicious/direct score writes fail, retry/finalize is idempotent.
- [x] Duplicate nickname/edit/delete/report, shared rank and offline behavior verified with evidence.
- [x] Server keys stay server-side and free service/retention/admin operations are documented.
- [x] P03 receives explicit privacy and native verification requirements.

Completion evidence: `docs/ranking-release-handoff.md` maps each criterion to actual hosted/SQL/browser/restore evidence and carries capacity, CAPTCHA, native and full-service disaster-recovery limits explicitly. Manual not_run entries in individual smoke reports remain unchanged; they are not whole-task verdicts.

## Validation
- `npm.cmd run ranked:check` / `npm.cmd run test:ranking` / `npm.cmd run server:check` — existing checks pass.
- `npx.cmd supabase --version` / `npx.cmd supabase functions deploy --help` — verify supported CLI/remote bundling.
- `npx.cmd supabase login` only when required, then `npx.cmd supabase projects list` — actual allowed account/project.
- Read actual ref from protected local config, then `npx.cmd supabase link --project-ref $nyangProjectRef`, `npx.cmd supabase db push`, `npx.cmd supabase functions deploy leaderboard-api --project-ref $nyangProjectRef` with verified remote-bundling flag if needed — intended migration/deployment success. Never use db reset against hosted data.
- `npm.cmd run ranking:verify -- --environment staging` and later `--environment production` — define script `node scripts/verify-leaderboard.mjs`; real success including scoped cleanup, no server secret logged.
- `npm.cmd run ranking:env-check` / `npm.cmd run typecheck` / `npm.cmd run test:ci` / `npm.cmd run web:export` / `npm.cmd run e2e` — matching config/client integration pass.
- `git diff --check` — clean. Known golden fixture digest compared across Node/browser/Deno; Hermes pending only until P03-T03.

## Commit Message
```text
feat(ranking): verify hosted leaderboard and guest data lifecycle

Plan: 2026-09-21-close-call-nyang
Phase: P02-leaderboard
Task: T03-hosted-verification

- Deploy and verify actual profile, ranking and protected score endpoints
- Document service limits, privacy changes and moderation operations
```

## Progress
- FINAL2026-09-25: acceptance criteria satisfied. FreshJest659/44suites, Deno50, tools67, SQL27, typechecks/ranked8,3webbuilds+localenvscan, Chromium34pass/11intentional-skip/0fail/0flaky(~3.5min),3numericgoldens passed. Staging/production read-only finalGET200/CORS/rulesmatch, users/game/pending0, migrations2/activecron/successrun>=1. No freshremote testusers. P03 privacy/native/publicweb requirements in docs/ranking-release-handoff.md. Historical below-pending entries are superseded. Complete task commit followed by pointer sync; no auto push.
- Recovery followup validation: ranked8/schema27/ranking186/12suites/servercheck/typecheck rerun/scratchDeno/diff passed. Initial typecheck picked scratch Deno.ts via Expo globs; scratch-only .mts rename resolved, app config unchanged. FullJest/browser/native not rerun. Task still in_progress awaiting final acceptance review/commit.
- 2026-09-25 recovery: Docker moved D:/어치/DockerDesktopWSL, existing15containers preserved. IsolatedPG17.6/Auth2.197.0 with explicitloopback. Realstagingguest1/legalshortscore0 -> single Auth/game/migration archive encrypted DPAPI -> decrypt/checksum -> transactional owner-preserving localrestore passed. Initial equality included dynamic fetchedAt and failed; source DELETE/Auth403 cleanup completed. Local-only followup5passed: record match/RLS6/RPC12service+0clients/FK/migrations, same archived Auth refresh->localJWT/getUser, realhandlerprofile/board/401/validreplay, delete/Auth403/allgame0. Remote independent15:29:41UTC Auth/game/pending0/completed18/buckets77. All recovery containers stopped; ports closed. docs/backup-recovery.md has exact scope/reports/limitations/retention. Actual whole-cloud/key/Storage recovery and native NOT claimed. Final T03 evidence review/handoff+completion commit still pending.
- 2026-09-25 local recovery preflight: Docker data VHDX on C:, host free2633674752bytes (~2.45GiB), D: free999256207360bytes. Existing15 stopped omokgo containers/images/networks preserved. Ports55320..55329 free. No pulls/local stack/remote writes/backup/restore performed. Pause large downloads pending user choice of supported Docker disk relocation to D: or user-managed C: free space; no automatic global settings change/prune. Actual recovery remains unverified; in_progress/no completion commit.
- Docker prerequisite correction: user reported already installed. Elevated registry/path lookup found per-user DockerDesktop under LocalAppData/Programs. Existing app launched hidden; Server Engine29.7.2/Desktop4.90.0 Linux amd64/WSL2 reachable,18CPU/8071057408bytes, no running containers. No install/WSL change/container creation/cloud writes. Supersedes absent-Docker/install-choice blocker. Next isolated Supabase setup; actual data/Auth restore still pending, T03 in_progress/no completion commit.
- 2026-09-24 recovery preparation: SELECT-only/read-only staging preflight14:39UTC confirmed Auth27tables/users/identities/sessions/refresh0, players/bests/runs/reports0, pending deletion0/completed17/rate buckets72, Auth FK and2migrations. Added preflight SQL/+2static tests, schema27passed. No dump/restore/new guest/remote write. Docker absent; native PG alone cannot prove Auth HTTP recovery. User installation/defer choice pending. docs/backup-recovery.md defines isolated target/security/real login/cleanup gates. T03 in_progress/no completion commit.
- 2026-09-24 production retention completed: isolated remote config dry-run verified jwt_expiry3600 without config writes; reviewed cleanup function body matched digest. Installed pg_cron and daily job1 `nyang-production-rank-cleanup`. First scheduled run1 succeeded14:34:00.084849..092564UTC; temporary calendar-minute schedule restored to active `15 3 * * *`/GMT (12:15KST), command cleanup3600. Post Auth/player/best/run/pending-delete0, complete tombstones2 preserved. No production synthetic data/whole-user deletion or staging change. Guarded operation SQL +2 regression tests; total schema25/ranking186/typecheck/ranked/server/diff passed. Full data/Auth restore and final handoff still pending, no completion commit.
- 2026-09-24 user Usage screenshot received: Free quota not exceeded for displayed billing cycle; DB0.027/0.5GB(5%), Edge437/500000, egress0.002/5GB, cached0/5GB, MAU0/50000, third-party MAU0/50000, storage0/1GB, realtime peak0/200/messages0/2000000. Screenshot omits org/project/date filter; record as user evidence, not independent API evidence or load test. Usage screenshot wait resolved; production cron/JWT retention and full data/Auth recovery still open. Documentation only, no new runtime tests or server changes; in_progress/no completion commit.
- 2026-09-24 operational continuation: read-only staging/prod PostgreSQL17.6 DB sizes11988115/11578515bytes, Auth0/player0 each. Staging cron1.6.4 active daily0315UTC cleanup3600; production pg_cron absent, so production scheduling/first run remains open. Discovered native pg_dump/pg_restore18.6 installed outside PATH. Staging private/public schema-only export (CLI login + SET ROLE postgres) restored to disposable localhost55439 PostgreSQL18 with empty synthetic auth.users and prerequisite roles, no ownership preservation. Audit6tables/6RLS/12RPC/service12/anon0/authenticated0, empty board passed. Local cluster stopped; remote DB unchanged. Does NOT verify actual data/Auth/session restoration or full provider recovery. Artifacts `output/schema-restore-781ae9c9e54b43329ee4d8ef560c847c/`. User will send Usage screenshot next chat; dashboard runtime unavailable. Keep in_progress/no completion commit.
- 2026-09-24 14:02 UTC: actual staging Chromium automatic offline recovery passed without manual retry/focus/reload/API mocking. One fresh UI signup, accepted live run, offline fall with persisted terminal proof and failureCount1; reconnect automatically chunk/finalize200 within2012ms, receipt matched local score0, proof cleared. UI deletion HTTP200/deleted=true and local session/deletion cleared; same-token DELETE200/Auth403 established cleanup. Report `output/staging-browser-auto-retry-20260924.json`:8passed, cleanupRequired0, signup uncertaintyfalse. Fresh isolated export/env10 and ranking186/12suites, ranked sync, Deno check, typecheck passed. Short zero-score fall tests transport/replay flow, not long-run/high-score/phone behavior. This closes the prior no-click browser gap; Usage/backup-restore/remaining operations still pending, no completion commit.
- Latest preparation evidence (2026-09-24): inspected hosted production report `14c82633-59f9-42b2-87d4-f82a739f651b` from13:38:09UTC: 14pass/0fail/8manual not_run, cleanupRequired0, signup uncertaintyfalse, smokePassedtrue. Production anonymous signup/replay/access control and own-user deletion verified. Isolated production web export `output/nyang-production-preflight-20260924` passed all10 env checks and local HTTP index/base/JS/17assets. Production direct GET200/CORS/rules match. Staging GET503 resolved after restoring only environment=staging and both localhost origins; salt untouched, both origins now200. Fresh GitHub repo/main verified; Pages404/variablesempty. Browser automatic retry, Usage/backup-restore and remaining operations still pending; task is not complete.
- 2026-09-24 10:14 UTC: actual staging lifecycle fixture passed via CLI 2.117.0 `db query --linked --project-ref tadokcpealpwjfyjovuy --file scripts/sql/ranking-staging-lifecycle.sql`. assertionsPassed/rollbackCompleted/intentGucsCleared=true; active visibility, hidden/banned filtering and write denial, banned read denial, expired-run rejection passed. Independent before/after Auth/player/best/run and fixture counts all zero. User manual execution no longer needed. Single-transaction SQL semantics only; concurrent sessions/HTTP and remaining operational checks still pending. Cron remains active with no visible run history.
- [ ] 구현 완료
- [ ] 검증 통과
- commit: pending
- external prerequisites: 사용자 선택1로 기존 프로젝트 재사용 승인. 실제 조직 Free/Owner/슬롯과 빈 DB 확인 후 nyang-staging(tadokcpealpwjfyjovuy) 재사용·nyang-production(fgojrxmpxpzdiwsktjsx) 생성 완료. 모두 서울/Free. staging만 서버 배포·부분 검증; production은 생성만 완료. 로그인/가입/프로젝트 선택을 다시 요청하지 않는다. 유료 전환은 승인하지 않음.

## Hosted Progress (2026-09-22, partial)

- staging migration202609210001/Edge 배포 완료. 실제 CASE 구문 오류는 최초 실패 트랜잭션 롤백 확인 뒤 괄호로 수정해 적용했다. 빈 DELETE 스트림을 실제 gateway가 전달하는 문제는 bounded 읽기 검증으로 수정·재배포했다. 인증을 완화하지 않았다.
- 익명Auth 활성/ES256/JWT3600초/가입30회·시간 확인. CAPTCHA는 기존 false 유지, 봇방지 완료로 주장하지 않는다. 서버 salt는 Secrets에만 저장, production DB 암호는 Windows Credential Manager 전용 항목에만 저장. 앱 env는 공개 URL/publishable key만 포함하고 Git 제외.
- 실제 카탈로그6테이블/12RPC/8helper, 역할별 SQL 거부96개, HTTP smoke14개(REST 비노출/RPC42501·정상 proof·중복·개명·신고·삭제 포함) 통과. 별도 staging101fixture에서1,1,3/top100/me101 통과·롤백. 최종 Auth/프로필/점수/판/신고0, 삭제 완료표식4만 의도적으로 보관. 전체 Task 통과가 아니다.
- 로컬 typecheck/ranked/server 통과, ranking184/Deno50/tools64/SQLstatic22/Chromium골든3 통과. Jest 최초583중1개 애니메이션 실패 → 단독33통과 → 변경 없는 전체 재실행583통과. 실패 원인은 확정하지 않고 QA에 보존한다.
- 미검증: 다른 판 최고값·동점/만료·운영자 변경 경쟁, Gateway/한도, 정리cron/백업, 실제 설정 웹/오프라인 복구, production 롤아웃·smoke, iPhone/Hermes. Acceptance/Progress 체크박스와 완료 커밋·다음 포인터는 그대로 미완료. 상세 증거는 운영/QA/학습노트 참조.
- 2026-09-24 fresh hosted smoke: local rule sync `nyang-v1-bc732af6f2a7ea66`, isolated staging web export and env boundary pass; actual staging runner 14 assertions pass, both temporary auth users deleted, no uncertain signup. A sandbox timeout on the first attempt was distinguished from the successful network-enabled retry. Concurrent duplicate-finalize passed; cross-run max/tie race and manual hosted cases remain not_run. Scheduled cron's first execution still pending.
- 2026-09-24 deep hosted cross-run check: staging-only `--verify-cross-run-concurrency` passed. Two separately paced, legal 101m proofs from separate temporary users finalized together and received the same rank with correct board order; a later lower-score proof preserved that user's 101m best and original `achievedAt`. `test:ranking-tools` passed 66/66. This is not a simultaneous same-player attempt test: `rank_start_run` invalidates the prior run for that player, so only cross-user tied finalization and subsequent max-preservation are established. Runner cleanup succeeded for both accounts. Still not verified: expiry/moderation races, Gateway/provider limits, scheduled cron's first execution, backup/restore, live web recovery, production readiness, iPhone/Hermes. Do not mark task complete.
- 2026-09-24 first cron history check (user-provided SQL Editor output): job 1 exists; LEFT JOIN to `cron.job_run_details` returned NULL for runid/status/times/result. No scheduler execution evidence yet; not a failure record. Keep cron validation pending for the next scheduled run.
- 2026-09-24 next staging test prepared: `scripts/sql/ranking-staging-lifecycle.sql` creates four random Auth stubs and isolated synthetic data inside one transaction, checks active/hidden/banned board and write behavior plus expired-run rejection, then requires ROLLBACK. `test:ranking-schema` 23/23 is static-only. User must run the whole file in staging SQL Editor after verifying ref; no hosted result yet.

## Preparation Evidence (2026-09-22, not completion)

- `scripts/verify-leaderboard.mjs`와15개 모의 회귀, `scripts/inspect-public-env.mjs`와45개 회귀를 준비했다. package 명령, 공개 대상 메타데이터 예시, CLI 임시파일 제외, 운영/개발/QA/학습 노트를 추가했다. 앱/서버 원본·물리는 변경하지 않았다.
- 전체60도구검사, Jest582(40 suites)/ranking184, typecheck/ranked:check/server:check, Deno45, Node/Deno/Chromium 공통3골든 통과. 기존 production dist local-only 정적 검사 통과; 실제 URL/key 빌드 및 새 전체 E2E는 미실행.
- CLI2.117.0에서 `--use-api`, `db push --dry-run --skip-vault` 확인. `projects list`는 LegacyPlatformAuthRequiredError로 중단됐다. 후속 답변에서 계정 없음으로 정정됐으므로 가입 후 로컬 `npx.cmd --yes supabase@2.117.0 login`을 진행한다. 토큰을 채팅에 받지 않는다.
- 설정 없는 hosted runner exit2/not_run과 기본 env 검사 exit1로 안전 차단됨. 생성/배포/호스팅 검사를 실제 실행하지 않았으며 관리자 키/실제 공개 설정도 만들지 않았다.
- runner는 명시적 환경/ref/상대ref/쓰기 플래그, 정상시간 proof 제출, 생성한 사용자만 정상 DELETE, 모의/실제 구분과 항상 taskComplete=false를 유지한다. 별도 읽기 전용 리뷰에서 새 차단 결함 없음. 상세 증거는 docs/qa-report.md와 docs/learning-notes.md 참고.
- 실제 DB/RLS/직접 RPC·공동순위101명·다른 판 최고값 경쟁·만료/운영/실제 웹/기기는 여전히 미검증이다. legacy/new server key·Gateway forwarded 신뢰·JWT 최대 수명/cron은 실제 환경 확인 후 필요한 변경만 수행한다.
- Acceptance/Progress는 미완료로 유지한다. 현재 변경은 완료 커밋/푸시하지 않았으며 P03으로 전진하지 않는다.
- 후속 명시 요청으로 표시 제목만 우당탕탕 냥대리로 변경한다. [제목·계정 정정](../../../decisions/2026-09-22-title-and-account-correction.md)의 결정을 따른다. 기술 식별자/게임 규칙/실제 검증 완료 여부는 바꾸지 않는다.
- 제목 변경 검증: 관련 단위48개/4 suites, typecheck, 새 web export, 새 dist의 저장·공유 Chromium9개(39.0초), local-only env scan 통과. 실제 서버 작업은 가입부터 대기하며 현재 T03 미완료를 유지한다.

## Login Check Follow-up (2026-09-22)

- 가입 대기는 해소됐다. 실제 CLI2.117.0 `projects list`/`orgs list` 성공: org oxbvynubycrowcazoqzx(Jeong Insoo), 기본 이름 프로젝트 tadokcpealpwjfyjovuy, 서울 ap-northeast-2, ACTIVE_HEALTHY, linked=false. 이 조회는 Auth 게스트/API/DB 동작 검증이 아니다.
- CLI의 조직 하위 명령은 `orgs`다. `organizations list`는 UnknownSubcommand로 실패했고 실제 help 확인 후 `orgs list`를 사용했다. 목록만으로 요금제/슬롯/프로젝트 용도/빈 DB 여부를 확정하지 않는다.
- 기존 프로젝트를 냥대리용 staging으로 사용해도 되는지 한 가지 질문을 보냈다. 답변 전 기존 프로젝트 이름 변경/연결/마이그레이션/생성은 하지 않는다. 실제 조직 plan과 무료 제한을 다음 읽기 전용 점검에서 확인한다.
- 현재는 메모리/운영 기록만 갱신했으며 구현/호스팅 테스트를 새로 통과했다고 주장하지 않는다. 외부 데이터 쓰기/키 조회/생성/배포/과금/푸시는 수행하지 않았다.

## 2026-09-24 · Latest staging evidence

- Pending lifecycle SQL was executed on staging using Supabase CLI 2.117.0 and the explicit staging project ref. Assertions passed; rollback and intent-GUC cleanup were true. Independent before/after Auth, players, best_scores, and runs counts were zero.
- Read-only function listing showed `leaderboard-api` ACTIVE v4 with Gateway `verify_jwt=false`. Protected user-token validation remains in the handler; prior hosted smoke rejected anonymous writes and invalid JWTs.
- Added staging-only `--verify-start-rate-limit`, incompatible with cross-run concurrency test. Real run: 30 `/runs` starts within one minute, then HTTP 429 `RATE_LIMITED` with positive `Retry-After` <=60s. Tool regression tests: 67/67.
- Report `4934e936-1665-48a7-acde-aa95f863f413`: smokePassed=true, cleanupRequired=0, signupResponseUncertain=false, taskComplete=false. Post-run Auth/players/best_scores/runs/reports=0; rateBucketCount=37 (<24h), oldRateBucketCount=0.
- Not proven: provider/Gateway quota or CPU/memory ceiling, forwarded-IP last-hop trust, scheduled cron run, backup/restore, hosted-browser offline recovery. Production was untouched. T03 remains `in_progress`; no commit yet.

## 2026-09-24 · Cron / Gateway / browser verification and cleanup discrepancy

- Cron `nyang-staging-rank-cleanup` was temporarily scheduled every minute in staging only. `cron.job_run_details` recorded runid=1, `succeeded`, `1 row` at 10:34 UTC. The original `15 3 * * *` daily schedule was restored immediately and is active. No production cron change.
- Original XFF test failed its expected shared bucket: 61 requests all returned 200 and produced 12 hashed guest-read buckets. This alone did not prove the cause. Deployed code now uses Supabase Gateway `cf-connecting-ip`, never caller-supplied XFF. Deno50/50, server check and diff-check passed; staging function deployed v5. Repeated spoof-XFF test: first60 200, 61st 429 with Retry-After40. A client-supplied CF header probe was Gateway-rejected 403 and was not counted as a pass.
- Isolated staging-config web bundle/env scan passed. Real Playwright browser from allowed local origin successfully used actual staging Auth/ranking. Network loss during accepted run displayed pending; user-triggered retry submitted chunk/finalize (200). Offline-start stayed local with no backfill after reconnect. Automatic retry without a user click remains unverified; GitHub Pages/iPhone/Hermes remain untested.
- CLEANUP BLOCKER: post-browser-run verification found Auth6/Profile5/Best1/Run1/Report0. Five synthetic `검증냥` profiles and one profile-less Auth account from the wrong-origin signup attempt are listed with exact IDs in QA/operations notes. Automation falsely treated confirmation button disappearance as deletion success; no deletion receipts exist. Browser auth sessions have been disposed, so do not use arbitrary SQL or delete other users. Owner must delete only those six IDs in **nyang-staging > Authentication > Users**, then provide read-only post-counts.
- Free plan backup export cannot run here yet: neither Docker nor `pg_dump` is installed. Dashboard Usage/actual provider limits need Dashboard access. CUA runtime failed to start; local Playwright worked. Cleanup, Usage, backup and auto-retry remain; T03 incomplete, no commit.
- 2026-09-24 follow-up: local rankedSession retry suite passed 28/28 (simulated retry/backoff/Retry-After/foreground boundaries), tools 67/67, Deno check and diff-check passed. This does not prove automatic retry in a live browser. Recheck of Supabase CLI `projects list` stalled 30 seconds without output; Dashboard CUA still fails to start, so current staging counts/Usage could not be refreshed. Latest known DB aggregate remains Auth6/Profile5/Best1/Run1/Report0; owner cleanup of the exact six IDs remains the next prerequisite. No commit/advance.
- 2026-09-24 follow-up: staging public leaderboard GET using only the existing public config failed before receiving a response (`fetch` TypeError); no credential was printed and no remote state was changed. Added an exact-ID read-only post-cleanup query to `docs/leaderboard-operations.md`, including separate pending-vs-completed deletion tombstone counts. Dashboard/CLI/network access and owner cleanup remain prerequisites. No commit/advance.
- 2026-09-24 continuation: Node fetch and PowerShell HTTP GET to public staging leaderboard both failed before HTTP response (network TypeError/WebException); no remote result. Whole local Jest passed 44 suites/659 tests; `typecheck`, ranked sync (`nyang-v1-bc732af6f2a7ea66`), ranking tools 67/67, Deno server check passed. Official Supabase docs rechecked and Free usage/pausing/backup limitations documented, including default `db dump` exclusion of managed Auth/Storage schemas and data/roles. Live quota/cleanup/backup restore/production rollout remain unverified; no commit.
- Connectivity diagnostic: DNS for the exact staging host resolves two A records, but Windows TCP 443 test returns false. This execution environment currently cannot reach hosted Supabase over HTTPS. Do not treat missing HTTP data as evidence of an application/server failure.
- Prepared `scripts/sql/ranking-staging-cleanup-check.sql` and a copyable PowerShell CLI invocation in operations docs. The script is SELECT-only and filters only the six exact synthetic IDs. It is intended after user removes them through Dashboard Authentication; do not treat generating this query as cleanup completion.

## 2026-09-24 · User-run staging smoke follow-up

- Sanitized report `output/ranking-staging-3eefa6e3-4769-4abd-af45-e3b6f2b72de4.json`: 14 passed, 0 failed, 8 `not_run`, cleanupRequired=0, signupResponseUncertain=false, taskComplete=false. This confirms the *two guests generated by that run* were removed through normal API cleanup; it does not confirm the earlier six browser-generated IDs were removed.
- User ran the runner successfully from PowerShell, so staging CLI/API works on their PC even though the agent environment's TCP 443 is blocked. Next: get exact-ID post-cleanup counts and Usage from their PC/Dashboard. Only after zero lingering test data and all staging acceptance checks should production migration/function/smoke be applied. Backup/restore, live automatic browser retry and production evidence remain outstanding. Do not mark done or commit.
- User supplied the exact-ID SELECT result: `auth_users=6`, `players=5`, `best_scores=1`, `runs=1`, `related_reports=0`, `pending_deletions=0`, `completed_tombstones=0`. The six documented synthetic staging Auth IDs are still present (five with profiles and one Auth-only); there are no pending/completed deletion records. This is current post-smoke evidence, not cleanup success. Owner must delete only those six through the staging Dashboard Authentication > Users, then rerun the SELECT-only query. Avoid further write tests until clean. T03 remains in progress and must not be committed/advanced.
- 2026-09-24 cleanup resolution (user-reported): project reference matched staging `tadokcpealpwjfyjovuy`; user located and deleted all six exact IDs through Dashboard Authentication and reports the subsequent SQL query returned zero. This supersedes the earlier lingering-user count; no deletion SQL was used. Production config-only preflight passed for `fgojrxmpxpzdiwsktjsx` and deliberately stopped before network/write at `TEST_WRITES_NOT_AUTHORIZED`. Docker/pg_dump unavailable. Still pending: account Usage evidence, a defensible backup/restore exercise, live no-click browser retry, and production migration/API/smoke. No commit until acceptance evidence is complete.
- User production `db push --dry-run --skip-vault` output names exactly `202609210001_leaderboard.sql` and `202609230002_leaderboard_top30.sql` for ref `fgojrxmpxpzdiwsktjsx`. Review: initial migration creates the private ranking schema/tables and restricted RPCs; second replaces board function with limit30. No DROP/TRUNCATE, and cleanup is only defined, not executed by migration apply. Static schema23/23, ranked sync, Deno check, production env-check, diff-check pass. Actual apply/deploy/smoke still pending; keep task in_progress.
- User reports `Finished supabase db push.` for production. The visible message did not enumerate applied version rows; request a read-only `supabase_migrations.schema_migrations` check in the same production ref before function configuration/deploy. CLI migration-list help stalled without output in the agent runtime, so no independent remote verification occurred. No function/API smoke or commit yet.
- User ran the production migration-history query and provided both expected versions: `202609210001`, `202609230002`. Record this as user-provided hosted evidence for ref `fgojrxmpxpzdiwsktjsx`. Next: run the read-only catalog audit against production, then configure/deploy function and perform scoped smoke. No actual function/API request or production smoke yet.
- User attached the production `ranking-hosted-audit.sql` output. Parsed evidence: migration versions match; all 6 expected private tables exist, RLS enabled, 0 policies/direct role grants; 12 RPCs exist with SECURITY DEFINER, empty search_path, no PUBLIC execute, execute only by service_role; 8 helpers have empty search_path/no execute grants, with only rank_cleanup SECURITY DEFINER; schema has no PUBLIC grant; no unexpected private relations/ranking routines. Next check live `rank_get_board` limit30 definition, then configure Auth/secrets and deploy production Edge Function. Production smoke remains pending.
- User confirmed production `rank_get_board` definition check returned `true` for the Top-30 cap. Production DB read-only checks are complete; next confirm anonymous Auth and Edge Function secret *names/configuration only*, deploy `leaderboard-api`, and run a production-scoped disposable smoke. Never display/request secret values. T03 remains `in_progress`.
- User ran `npx.cmd --yes supabase@2.117.0 secrets list --project-ref fgojrxmpxpzdiwsktjsx`; the table was empty, confirming no custom production Edge Function secrets yet. Next configure `RANKING_RATE_LIMIT_SALT`, `RANKING_ENVIRONMENT=production`, and `RANKING_ALLOWED_ORIGINS=https://jeong-insoo.github.io`; do not configure Supabase-injected `SUPABASE_URL` or service-role key manually. Check anonymous Auth before deploying.
- User confirms those three production secrets have now been saved and anonymous sign-ins are enabled. Secret values were not shared. Next deploy only `leaderboard-api` to production, confirm it is active with `verify_jwt=false`, then run the scoped production smoke. T03 remains in progress.
- User reports `functions deploy leaderboard-api` succeeded for production ref `fgojrxmpxpzdiwsktjsx`, uploading expected source assets. This is deployment output, not yet runtime/API verification. Next list remote functions and check ACTIVE/version/`verify_jwt=false`, then run scoped production smoke.
- User-provided production function list shows `leaderboard-api` ACTIVE version 1, updated 2026-09-24 13:05:41 UTC. The table omitted `verify_jwt`; confirm via JSON listing/Dashboard before production smoke. Smoke remains pending.
- User confirms production `verify_jwt=false`. Next run only the production-scoped `ranking:verify` smoke with explicit production ref/env and `--allow-test-writes`; inspect passed/failed/not_run and cleanup flags. Protected endpoints still validate Auth in-handler. T03 remains incomplete until actual smoke and remaining acceptance checks.
- First production smoke attempt reported `PUBLIC_BOARD:failed`. This assertion precedes all signups, so a stop here creates no test users/score rows. Exact report evidence is pending; inspect sanitized check IDs/status/evidence, cleanup count and signup uncertainty before diagnosis. Do not rerun yet.
- User supplied sanitized report: `PUBLIC_BOARD=UNEXPECTED_HTTP_503`; subsequent checks not_run (fail-fast), cleanupRequired=0, signupResponseUncertain=false, smokePassed=false. No test writes occurred. Code permits 503 from invalid startup config (missing SUPABASE_URL/service-role key, missing/short salt, invalid origins) or unexpected DB/RPC errors mapped to UNAVAILABLE. Root cause undetermined; check configured secret names/constraints before retry.
- Follow-up CLI `secrets list` showed only platform `SUPABASE_*` values, but user reports all three custom names are present in Dashboard Edge Functions > Secrets. These sources conflict; withdraw the conclusion that salt absence is established. Verify Dashboard project ref and values (salt length, exact environment/origin) and inspect runtime evidence before retry. No smoke writes occurred.
- User identified Dashboard ref as staging `tadokcpealpwjfyjovuy`; CLI/smoke targeted production `fgojrxmpxpzdiwsktjsx`. This resolves the conflicting secret listings and explains the missing production salt/startup 503. User subsequently reports settings saved after switching to production. Still verify production anonymous sign-ins (prior confirmation may concern staging), then rerun production smoke. Successful runtime/cleanup remains unproven. Inspect staging settings for accidental production environment/origin values before future staging checks; no such change is confirmed yet.

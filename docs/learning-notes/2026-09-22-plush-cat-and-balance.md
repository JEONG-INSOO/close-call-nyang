# 2026-09-22 · 인형형 냥대리와 빠른 균형

## 무엇을 / 왜 바꿨나

사용자의 첫 플레이 피드백은 ‘긴 사람 다리를 짧은 고양이 팔다리로’, ‘기울어짐은 빠르게, 실제로 많이 기울어진 뒤 넘어지게’였다. 확정된 1번을 `memory-plan`의 단일 청사진으로 만들고 `memory-execute`로 외형·물리·재생 검증을 함께 구현했다. 사진은 체형 참고이며 기존 크림색/네이비 정장과 세 표정을 유지했다.

- `src/scene/NyangCharacter.tsx`: 머리 그림은 19 아래로 옮기고 몸은 넓혔다. 바지·검은 구두를 없애고 18 높이 크림색 발과 둥근 앞발을 그렸다. 캐릭터 전체 기준은 205→186, 머리118/몸50/발18이다. 걸음은 ±8도·좌우2·들기3으로 짧게, 컵은 `(62,-39)`로 이동해 발과 연결했다. 세 캐릭터의 ID/의상/표정/해금은 그대로다.
- `src/game/balance.ts`, `difficulty.ts`: 실제 회전을 빠르게 만들고 실패 각도를 약40.1→65도로 넓혔다. 감쇠는 움직임을 누르는 힘, 조작 가속은 각속도에 입력을 더하는 힘이다. 실패 각도만 넓히면 느린 조작이 더 쉬워지므로 함께 조정했다.

```ts
criticalAngleRad: 1.134464014, // 약65°, 재생 정밀도 1e-9에 맞춤
controlAcceleration: 9.6,   // 기존4.8
damping: 2.4,               // 기존3.2
// difficultyAt: instability 4.8 + 0.9 * level, disturbance 0.22 + 0.16 * level
```

- `src/game/__tests__/balance-feel.test.ts`: 예전 커널의 동일 입력 결과와 비교하고 양방향 회복, 동시 버튼 상쇄, 자동 중앙 복귀 없음, 임계각 즉시 실패를 검증한다. 기존 engine 테스트도 양쪽 임계선 실제 통과/부분 틱 점수/fall 1회를 검사한다.
- `scripts/generate-ranked-replays.mjs`: 같은 시드와 실제 좌/우 입력만으로 골든을 재생성한다. 기본 실행은 요약만, `--check`는 기존 파일과 비교만 하고 `--write`를 명시해야 저장한다. 장거리 테스트 운전자는 테스트 전용이며 게임에 자동 균형 기능을 넣지 않았다.
- `e2e/fixtures/SceneFixtures.tsx`, `fixtures.spec.ts`: 3종×커피2×포즈7 = 42개를 두 화면 크기로 검사한다. 정적 합성 화면은 실제 플레이·iOS 스크린샷·100% 달성 증거가 아님을 표시한다. 실제 키보드 테스트는 실패 전 화면 각도가50도를 넘는지도 별도 측정한다.

## 수치와 보존한 규칙

시드1/42/241/7654/98765에서 같은 0.2초 오른쪽 입력의 각도 변화는 기존0.0791~0.0847rad에서0.1677~0.1784rad로 약2.11배였다. 이 숫자는 짧은 고정 입력 비교이며 모든 상황의 속도나 재미가2배라는 뜻이 아니다. 무입력 실패 시간은 각각8.826→4.915, 8.928→4.727, 9.248→4.250, 6.896→6.496, 5.376→2.679초로 빨라졌다. ±50도와 바깥 각속도0.5에서 반대 입력으로0.675~0.700초 안에 중앙 근처(절대0.1rad 미만)로 돌아왔다.

걷는 속도/15% 커피와 가속/51% 사무실/약90초100%/100% 이상 진행/저장 키/해금/선택은 바꾸지 않았다. 동시에 양쪽을 눌러도 무입력과 틱마다 완전히 같아서 결국 넘어진다. 캐릭터 그림 크기는 물리 입력이 아니며 세 캐릭터 능력은 동일하다.

새 규칙은 `nyang-v1-9fc10a2a8fdd4085`다. 원본과 생성된 서버 사본·클라이언트 규칙 ID를 동기화했다. 골든은 무입력780틱5%, 오른쪽65틱0%, 사무실10,955틱101%(커피/사건경고7회)다. Node/Deno/Chromium에서 같은 결과와 해시를 확인했다. 서버 파일 생성은 실제 클라우드 배포가 아니다.

## 검증 기록

- 장면51개, 게임134개 및 보완 회귀38개, Deno50개, 랭킹 도구64개, Chromium 골든3개 통과.
- 첫 전체 Jest는612통과/2실패였다. 새65도 경계를 옛 고정 각속도로 통과하지 못하는 보호 테스트와 옛 골든 틱 하드코딩이 원인이었다. 임계각 상대 속도 및 현재 fixture의 이름/틱/해시 정확 대조로 수정했다. 테스트 기대를 느슨하게 하거나 보호 로직을 바꾸지 않았다.
- 새 외형의 SharedValue 교체/커피 opacity는 비동기 애니메이션 갱신이 실제 반영될 때까지 `waitFor`로 정확값을 확인하도록 수정했다. `[frame]` 의존성도 모든 관련 opacity 구독에 명시했다.
- 최종 전체 Jest615/615(41 suites,29.095초), Chromium28통과/11의도된중복환경제외/0실패/0flaky(164.456초). 보호/진단 하드코딩 수정 후 재실행 결과다.
- 타입 검사, 서버 타입 검사, 세 웹 빌드, local-only 공개 설정 검사 통과. 기존4173 서버가 이 프로젝트 dist와 동일한 HTML을 제공함을 확인해 보존했다. 임시 Playwright 설정은 이 서버만 재사용하고 별도 두 QA 서버는 직접 관리한다.
- 844×390/667×375 합성장면 검사2개 통과. PNG에서 짧은 발·커피 접점·60도/82도 포즈를 직접 확인했다. 검토자는844화면의19장(성실 중립,3종×커피2×좌우60도12장,종류별 좌우82도6장)을, 메인은844신입 중립과667의신입60도/베테랑82도/전체장면 및실제플레이를 직접 봤다. 84개전체 PNG를 모두 육안검사했다고 주장하지 않는다.

## 알아야 할 점

실패 전 화면은 엔진 각도 그대로이며 65도에서 즉시 끝난다. 이후82도/240ms 넘어짐은 표시 전용이므로 점수/시간을 더하지 않는다. 신규 게이지·유예시간·자동 중심 복귀·치트 경로는 없다.

현재 dist는 온라인 공개 설정을 넣지 않은 로컬 테스트용이다. staging 클라우드는 여전히 옛 `nyang-v1-2093a8b42d416f8a`이므로 새 클라이언트를 연결하기 전에 원래 P02-T03에서 새 규칙 배포·검증을 해야 한다. production/Pages/App Store는 이번에 배포하지 않았다. 기존 사용자 저장/세션/서버 데이터를 지우지 않았다. iPhone/Expo Go/Hermes와 실제 손맛 평가는 아직 미검증이다.

화면 캡처와 테스트 산출물은 Git 제외 `test-results/`, `output/`에 있으며 다음 검증에서 교체될 수 있다. 현재 기록은 장시간 성능이나60fps의 증거가 아니다.

## 함께 발견했지만 범위 밖인 부분

- `src/theme/tokens.ts`에 옛205/118/51/36 치수 필드가 남아 있다. 실제 그림은 `NYANG_RIG`만 사용해 이번 화면에는 영향이 없다. 청사진상 read-only 파일이라 임의 수정하지 않았으며 후속 정리 시 중복 상수를 제거/통일하면 좋다.
- 이전 Supabase T03의 경쟁·만료·운영 검증/production 배포는 미완료다. 이번 성공을 서버 작업 완료로 처리하지 않는다.
- 공유 학습/QA/current 문서에는 기존 미커밋 서버 작업이 섞여 있어 그 내용을 이번 코드 커밋에 통째로 포함하지 않는다. 이번 단독 노트와 새 작업 문서/소스/검증만 커밋한다. 자동 푸시는 하지 않는다.

## 완료 기록

타입/서버타입/Jest615/Deno50/도구64/규칙사본/골든생성기 --check/Chromium재생3/세웹빌드/공개환경/브라우저28/공백검사 통과. 실제 키보드로 오른쪽을 유지한 판에서 실패 전 최대 관측각은 desktop62.08도,844화면64.39도,667화면61.61도였다. 프레임 표본값이지65도보다 일찍 실패한 수치가 아니다. 최종 각도의 정확 판정은 엔진 테스트에서 별도 확인한다. 양손 터치 독립 해제, 정지/화면회전, 재시작, 설정/최고점 재로드, 모의랭킹 복구도 통과했다.

기존 주소 `http://127.0.0.1:4173/close-call-nyang/`에서 HTML과새bundle이 dist 파일과동일함을읽기비교했고,새임계각/발SVG 존재와 fake.invalid 부재를확인했다. 빌드파일은 `index-b81aa7b4f07fc6e87ab3e4ba9aa13735.js`. 최종 임시 설정의 JSON보고서는 `output/test-results/results.json`, 캡처는 `test-results/`다.

현재 Task 코드를 로컬 커밋한 뒤 current는 원래 서버 검증P02-T03으로복귀한다. 해당서버작업완료/새클라우드배포/자동푸시는아니다. 변경요약이 `feat(game): add plush cat proportions and snappier balance`인커밋을참고한다.
